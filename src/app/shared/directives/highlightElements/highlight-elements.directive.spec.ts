import { HighlightElementsDirective } from './highlight-elements.directive';

describe('HighlightElementsDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightElementsDirective();
    expect(directive).toBeTruthy();
  });
});
