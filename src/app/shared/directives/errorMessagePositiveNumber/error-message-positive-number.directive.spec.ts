import { ErrorMessagePositiveNumberDirective } from './error-message-positive-number.directive';

describe('ErrorMessagePositiveNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorMessagePositiveNumberDirective();
    expect(directive).toBeTruthy();
  });
});
